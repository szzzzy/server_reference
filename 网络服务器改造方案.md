# 网络服务器改造方案(公网访问版)

> 目标:把当前"开发板 — USB 串口 — 5090 PC 本地流水线"改造成 **开发板经 WiFi/4G 通过公网访问 5090 语音服务** 的网络服务器形态。
> 本文档只做方案设计,不涉及代码改动。PC 侧(5090)实现由本仓库完成;开发板侧(ESP-IDF 固件)在外部工程 `espidf-mic-pcm-stream` 中完成。

---

## 0. 结论摘要

1. 当前链路是**本地 USB 串口**,不是网络服务;公网化必须新增**网络传输层 + TLS**,并解决 5090 所在网络的**公网可达性**。
2. 推荐形态:**单端口 TLS TCP 长连接 + 原二进制帧协议(帧内加握手/心跳/序号/认证)**,即"同协议、换传输"。流水线(VAD/ASR/Qwen3/CosyVoice2)不动。
3. 公网访问的三个前提,缺一不可:
   - 5090 所在路由有**公网 IPv4**(或 IPv6),否则需云中转/组网;
   - 域名 + **CA 证书**(Let's Encrypt 或阿里云免费证书),强烈建议 **mTLS 双向证书**防盗连;
   - 路由器端口转发 + DDNS。
4. 带宽/延迟完全可行:上行 16kHz·16bit 单声道 = **32 KB/s**,下行 24kHz·16bit = **48 KB/s**,公网 RTT 20–100 ms 时靠 50–100 ms 抖动缓冲即可稳定。

---

## 1. 现状梳理(改造基准)

### 1.1 PC 侧(本仓库)

`next_stage/voice_sleep_v5_5090/voice_daemon.py`(V5 两段式硬件唤醒):

- Windows 登录时预加载 Paraformer-online / Qwen3-4B / CosyVoice2 三个模型;
- 等待开发板插入 → 自动识别 COM 口(921600 baud)→ 录背景噪声校准 dBFS;
- 板端 **MICS(硬件休眠触发模式)** 发送阈值,板端本地做 120 ms 能量检测,仅触发后上传 PCM(前 500 ms 预录);
- 有唤醒词 → 进入对话:持续 MICW 采集 → VAD 端点 → FunASR → Qwen3(流式,按标点分句)→ CosyVoice2 → 下行回板播放;
- 无唤醒词 / 说"进入休眠" → 重新进入 MICS 硬件休眠。

### 1.2 板→PC 上行协议(`board_serial_asr_test.py`)

| 命令 | 格式 | 说明 |
|---|---|---|
| `MICS` + val(4B) | 命令 | 硬件能量触发模式,val = 背景 dBFS×100 |
| `MICW` + 0(4B) | 命令 | 连续麦克风上传模式 |
| `PCM1` 帧 | 4+4+2+2+payload | 魔数 `PCM1` + seq(u32) + payload_len(u16) + dbfs×100(i16) + payload(≤若干KB,i16 PCM 16kHz) + checksum8(payload) |

### 1.3 PC→板下行协议(`next_stage/full_pipeline_auto_5090/realtime_pipeline.py`)

| 命令 | 格式 | 说明 |
|---|---|---|
| `SPKS` + rate(4B) | 命令 | 启动板载扬声器(24000) |
| `SPKV` + percent(4B) | 命令 | 音量 |
| `SPKD` + len(4B) + PCM | 数据 | 音频数据块,每块 ≤2048 B |
| `SPKE` + 0(4B) | 命令 | 结束 |

**要点:协议是"命令 + 数据块"的简单二进制流,无会话、无握手、无校验(下行),链路状态全靠串口物理可靠性。** 直接搬到公网不可行(无鉴权、无重连、无拥塞控制),必须框架化。

---

## 2. 目标架构

```
                公网(WAN)
 ┌────────────┐   TLS(TCP 4433)   ┌──────────────────────────────┐
 │ ESP32-S3   │ ◄────────────────► │  5090 PC (家庭/办公室网络)     │
 │ WiFi / 4G  │                    │  ├ net_board_server.py(新增) │
 │ 板载麦克风  │                    │  │   - TLS/mTLS 终结          │
 │ 板载功放    │                    │  │   - 会话/鉴权/心跳/重连     │
 └────────────┘                    │  │   - PCM1/SPKD 帧编解码      │
                                   │  ├ voice_daemon.py(微改)      │
                                   │  │   - 传输换成网络适配层       │
                                   │  │   - VAD/ASR/LLM/TTS 不动    │
                                   │  └ Qwen3/CosyVoice2(本地)     │
                                   └──────────────────────────────┘
```

### 2.1 公网可达性(先决条件,必须先确认)

| 情况 | 方案 |
|---|---|
| 有公网 IPv4(运营商给独立 IP) | 路由器端口转发 4433 → 5090;DDNS 绑定域名 |
| 只有 IPv6 | 直接 IPv6 访问(需要 5090 有 v6 地址且路由器放行) |
| 无公网 IP(PPP 大内网/CGNAT) | 云中转(下文 2.3)或 Tailscale/WireGuard 组网,二选一 |
| 移动 4G/5G 场景 | 板端用 4G → 公网,与 5090 出口无关,但要考虑板端流量和网络切换重连 |

### 2.2 证书与域名

- **域名**:一个固定域名,必须做 **DDNS 自动更新**(路由器自带 DDNS,或 5090 上跑小脚本调域名商 API)。
- **证书**:Let's Encrypt 免费证书,推荐 **DNS-01 验证**(阿里云/Cloudflare API,无需向内网暴露 80/443,win-acme 或 Caddy 均可自动续期)。
- **为什么必须 TLS**:公网传输的是真人语音与引导词,HX 明文会被监听/伪造;ESP32 侧 mbedTLS 支持 TLS 1.2/1.3,内存足够。
- **强烈建议 mTLS(双向证书)**:给板卡发一张客户端证书,服务器只收持有证书的连接。比共享 token 更抗盗连,且公网扫描器无法重放。若嫌证书管理麻烦,至少用 **板 ID + 预共享密钥** 做第一层,证书做第二层。

### 2.3 无公网 IP 的备选:云中转(Tailscale 之外的方案)

```
板端 ──TLS──► 云服务器(轻量,如阿里云 2C2G)◄──TLS── 5090
             固定域名+证书在此终结          5090 主动出站连云端
```

- 5090 和板端都**主动出站**连接云中继,完全避开 NAT 问题,不用端口转发;
- 云上只做字节转发 + 会话管理(或轻量 MQTT/WS broker),不做语音推理;
- 代价:多一跳 ~20–40 ms、每月云服务器费用。**推荐先用 Tailscale 验证,再决定是否上云中转。** Tailscale:双向 mTLS 由 it 认证、免证书、免端口转发,唯一问题是其出口服务器路径,实测延迟通常可接受。

> 本方案主体按"公网 IP + 端口转发 + TLS"编写;若走云中转或 Tailscale,除传输接管方式外,其余协议/服务器设计**完全不变**。

---

## 3. 传输层设计

| 项 | 值 | 说明 |
|---|---|---|
| 传输协议 | TLS 1.3 over TCP | 单条长连接,双向全双工 |
| 端口 | 4433 | 避开 80/443 常见扫描;端口转发映射 |
| 服务器绑定 | `0.0.0.0:4433` | 只允许内网也可改 bind 内网 IP + 云中转 |
| 会话数 | 1 板卡会话 + 管理连接(可选) | 多板卡需会话 ID 区分,建议初期单板 |
| Keepalive | TCP keepalive + 应用层心跳 5 s | 双保险发现断链 |
| 代理解析 | 5090 出口侧可先加 Caddy/Nginx 终结 TLS | 服务器代码内做 TLS 更少组件 |

### 3.1 为什么用"TLS + 原帧协议"而不是 WebSocket/HTTP

- HTTP:请求/响应模型不适合连续音频双向流;
- WebSocket:可用,但对 ESP32 而言多一层帧解析,且没有比自研二进制帧提供更多价值;
- **原二进制帧 + TLS TCP**:固件端改动最小(串口收发换成 socket 收发,帧格式不变),延迟控制最直接。

---

## 4. 应用层协议 v1"网络版"(在现协议上增量)

> 原则:**旧帧格式一个字符不改**,只增加"会话层"封装。

### 4.1 会话握手(连接建立后,先做握手,之后才允许音频数据)

```
板→服务器: "NETH" + ver(1B) + board_id(32B) + nonce(16B)        (44B)
服务器→板: "NETH" + ver(1B) + session_id(16B) + nonce(16B)      (37B)
板→服务器: "NETA" + hmac_sha256(板密钥, ver+board_id+session_id+nonce)   (36B)
服务器→板: "NETO" / "NETE"(拒绝:原因码 1B)
```

- `board_id` = 板卡唯一 ID(烧录时写入 NVS);
- `hmac` 使用烧录在固件里的共享密钥(或在 mTLS 下可用客户端证书指纹替代 HMAC);
- 握手失败,服务器记录日志并断开(可做 per-IP 限速:同一 IP 5 分钟 3 次失败即拉黑 10 分钟)。

### 4.2 帧分封(所有旧命令/数据帧保持不变,外面加 4 B 头)

```
net_frame = magic(4B "NET1") + len(u32, 含 type 与 payload) + type(1B) + payload
type: 0x01 音频上行(PCM1 原帧 = payload)   0x02 控制命令(MICS/MICW/SPKS/SPKV/SPKE 原样为 payload)
      0x03 音频下行(SPKD 原样)             0x04 心跳                     0x05 心跳回执
      0x06 会话控制(握手/复位/重连说明)     0x07 服务器状态(非关键)
```

- 序号:三种数据流各自独立累加 seq(音频上行、音频下行、命令),接收方**忽略乱序旧帧,不作重传**(音频丢 1 帧=20 ms,重传反而加大延迟;靠抖动缓冲吸收);
- 心跳:5 s 一次;2 个周期无回执 → 本端判定断链,进入重连退避(1 s→2 s→5 s→10 s→30 s 封顶),恢复后**重新握手**,服务器复位该会话所有状态(校准 dBFS、对话历史由新会话重新积累);
- 命令是**可靠通道**(QOS):发命令后等回执(`0x06` ack,带原 seq),超时 1 s 重发一次,再失败按断链处理 —— 防止 "SPKV 没生效、MICS 没切换" 这类致命错乱;
- 流量控制:音频数据不带背压(简单),凭 TCP 窗口 + 板端音频缓冲水位自行丢弃旧帧;服务器下行 TTS 块按 `SPKD` 原序发送。

### 4.3 断线时板端行为(重要)

- 音频/对话进行中断线:板端立即停止 TTS 播放(防卡音),回到"本地能量检测"状态;
- 服务器端若检测到长静音+断开:结束当前会话,清空 LLM history,重新进入"等板卡"状态;
- 重连后默认进入"MICS 硬件休眠"——**必须重新校准**(见 6.3 简化项:首次握手成功后服务器自动发 `MICS`,板端重新录背景,避免把断网当语音)。

---

## 5. 服务器端(5090 PC)实施清单

### 5.1 新增模块 `next_stage/voice_sleep_v5_5090/net_server.py`

1. `asyncio` + `ssl.SSLContext`(TLS 1.3,服务端证书/私钥,`verify_mode=CERT_REQUIRED` 开 mTLS 时加载 CA 列表);
2. 会话状态机:`HANDSHAKE → AUTH → READY → ACTIVE → RESET`(复用 voice_daemon 的状态逻辑,不新建一套);
3. 帧编解码:`net_frame` 解包/封包 + 校验;内部调用现有 `board_serial_asr_test.py` 的 `checksum8`/`read_frame` 逻辑(此时 `read_frame` 的输入从串口换成 socket 字节流 —— 建议把 `read_frame` 重构为"从任意字节流对象读",串口/socket 都是字节流,一行核心逻辑都不用改);
4. 抖动缓冲:上行 20 ms 帧 → 排队 50 ms 再交 VAD;下行芯片不足时按时钟率吐帧;
5. 管理通道:可选加一个 `/health` 控制通道(HTTP 或复用 `0x07` 帧)供外部与网页看板查询状态;
6. 日志:每次会话握手/失败/断链/延迟统计写入 `runs/` 下与现有 CSV 统一的日志。

### 5.2 修改 `voice_daemon.py`(最小改动)

- 把 `open_serial(port, baud)` / `ser.read()` / `ser.write()` 全部替换为 `net_server` 提供的 `Transport` 接口(`send_command`/`send_audio`/`receive_frame`/`close`);
- **VAD、ASR、Qwen 流式分句、TTS 逻辑、提示音缓存、校准逻辑全部不动**;
- `config.json` 追加:

```json
"network": {
  "enabled": true,
  "listen": "0.0.0.0",
  "port": 4433,
  "tls_cert": "certs/server.crt",
  "tls_key": "certs/server.key",
  "tls_ca": "certs/ca.crt",
  "require_client_cert": true,
  "board_ids": ["board-01"],
  "heartbeat_seconds": 5,
  "jitter_buffer_ms": 50
}
```

- 保留串口路径(`enabled:false` 即回到现有串口模式),保证**双模式共存**,串口是公网故障时最快的回退。

### 5.3 基础设施

1. 5090 静态内网 IP;路由端口转发 4433;
2. DDNS 客户端(路由器自带或 5090 定时任务);
3. Windows 防火墙:`netsh advfirewall firewall add rule name="voice4433" dir=in action=allow protocol=TCP localport=4433`;
4. 证书目录 `certs/`(gitignore);
5. 续期:win-acme 或 Caddy 的 DNS-01 插件(推荐阿里云 DNS API),续期后**无需重启 daemon**(ssl.SSLContext 定时 reload 或子进程托管)。

---

## 6. 固件端(ESP32-S3)实施清单

### 6.1 网络层

1. `esp_wifi` STA 代理,NVS 存 SSID/密码;
2. `mbedtls` 客户端:主机名校验 + CA 固定(把 CA 根证书编译进固件,自签需 pin 证书链),开 TLS 1.3;
3. 域名解析(支持 DNS 轮询)、重连退避(见 4.2);
4. 若走 mTLS:客户端证书/私钥烧录进 NVS(建议使用 `esp_secure_cert` 分区,密钥不可导出);
5. 4G 场景(可选):SIM7600/EC800 模组走 AT + PPP/LTE,与 WiFi 共用同一套 TCP 栈。

### 6.2 音频路径(改动点)

- 上行:麦克风 I2S 中断 → DMA buffer → 组 `PCM1` 帧(原格式)→ net 发送;**保留本地 120 ms 能量检测逻辑**(这是"两段式唤醒"省电/省流量的关键,网络化后仍在板端本地做,不上传);
- 下行:收 `SPKD` → 环形缓冲 → I2S 播放;缓冲水位 < 50 ms 时向服务器发"UNDERFLOW"提示帧;
- **时钟**:建议板端 NTP 同步,音频帧带相对时间戳(`dbfs_x100` 字段保留,可在 net 帧 payload 里附加 `ts_ms(u32)`,服务器端会话记录中可直接划延迟)。

### 6.3 会话与校准

- 原生串口版假设"插上就能用";网络版必须处理:**新会话必须先校准**。握手成功后服务器发 `MICS` 并进入校准流程(现有 `calibrate_board` 逻辑复用);
- 固件需在烧录时写入 `board_id` + 共享密钥,并在 `sdkconfig` 开启 `CONFIG_MBEDTLS_SSL_CLI_C` / `CONFIG_MBEDTLS_SSL_TLS13`。

---

## 7. 延迟/带宽/资源核算

| 项 | 核算 | 备注 |
|---|---|---|
| 上行带宽 | 16000×16bit×1 ≈ 32 KB/s = 256 kbps | 20 ms/PCM1 帧 ≈ 640 B/帧,加 16 B 头 |
| 下行带宽 | 24000×16bit×1 ≈ 48 KB/s = 384 kbps | plus SPKD 头 12 B/2048 B ≈ 0.6% |
| 板→服务器 RTT | 局域网 1–5 ms;公网 20–100 ms;4G 40–120 ms | 实测为准 |
| 服务器抖动缓冲 | 上行 50 ms / 下行 80–120 ms(下行优先防爆音) | 下行可据 RTT 动态调整 |
| 总附加延迟 | 抖动缓冲 + RTT/2 ≈ 60–150 ms | 相对 USB 串口,≤200 ms 可感知但可接受 |
| ESP32 资源 | mTLS 握手峰值约 45–60 KB RAM(1.3.3 有段临时内存) | 需确认当前固件 heap;不够则降 TLS 1.2 |
| 服务器 CPU | 加解密 48+32 KB/s 可忽略 | 主要成本仍在 GPU 推理,不变 |

断线影响:音频上行丢帧 → 由缓冲吸收;单向丢包>5% 需改走 UDP/QUIC,但**终端口语场景优先级是正确性 > 最低延迟**,TCP 是正确选择。

---

## 8. 测试与验收

### 8.1 链路测试(实验室)

1. 用一根网线/WiFi 同网段:实测握手、心跳、重连、帧序;
2. 断网模拟:`shutdown`/禁用网卡 → 板端自动重连并重新校准 → 恢复对话;
3. 弱网模拟(可用 `tc` 或路由器策略):丢包 1%/3%、延迟 100/200 ms、抖动 ±50 ms,看 VAD 端点与 TTS 是否爆音;
4. 公网实测(家/办公室出口 + 4G 热点):连续 30 分钟对话,记录 RTT、丢包、重连次数;

### 8.2 效果对比(与串口基线比)

- 复用 `board_serial_asr_test.py` 的 CSV 流程,产出 `net_board_asr_results.csv`:same 8 句、同样 1 m 距离,CER、首字符延迟、首段音频延迟;
- 对比指标:唤醒延迟(触发→"我在"音频)、首字延迟、完整回答时长、TTS 卡顿次数;
- 验收红线:CER 与串口基线差 ≤ 0.02;无 TTS 爆音/卡顿;30 分钟内断线 ≤ 1 次且可自动恢复。

### 8.3 安全验收

- 无证书连接被拒;错误 client cert 被拒;
- 公网扫描器连接(随机 IP)不可导致 daemon 崩溃/异常日志刷屏;
- 证书过期前 30 天自动替换方案验证一次。

---

## 9. 里程碑

| # | 阶段 | 内容 | 工作量(人日) |
|---|---|---|---|
| M1 | 确认公网能力 | 确认家庭/办公室出口是否公网 IP;域名 + DDNS 准备 | 0.5 |
| M2 | PC 侧服务器 | `net_server.py` + transport 改造 + config + 证书 | 2–3 |
| M3 | 局域网联调 | 联网(不用 TLS)内网跑通两段式唤醒全链路 | 1 |
| M4 | 固件改造 | WiFi+TLS+会话+缓冲,wireless 外设 | 3–5 |
| M5 | 公网联调 | 端口转发/DDNS/证书/公网实测 | 1–2 |
| M6 | 测试验收 | 8.1–8.3 全部项 | 1–2 |
| 合计 | | | 约 8.5–13.5 人日 |

**PC 侧(M2、M3、部分 M6)可由本仓库直接实现;固件侧(M4)需要 ESP-IDF 工程配合。**

---

## 10. 风险与回退

| 风险 | 影响 | 缓解 |
|---|---|---|
| CGNAT 无公网 IP | 端口转发不可用 | 提前确认;走 Tailscale/云中转(2.3),服务器设计不变 |
| 公网抖动导致 TTS 卡顿 | 体验劣化 | 下行缓冲 120 ms 起,动态调整;极端场景降为"整句合成再播"模式 |
| 证书续期失败 | 服务中断 | 续期脚本 + 到期监控告警;mTLS CA 用长期 CA 证书 |
| ESP32 TLS 内存不足 | 无法握手 | 降级 TLS 1.2;或关 mTLS 改 token+IP 白名单 |
| 多板并发 | 资源占用叠加 | 初期单板;多板时每板一个 daemon 实例或会话队列(云中转方案天然支持) |
| 安全 | 公网被滥用 | mTLS+板 ID+失败限速+定期轮换证书;绝对不暴露 Qwen3/CosyVoice2 内部端口 |
| **回退** | 任何网络故障 | `config.json network.enabled=false` 一键回到串口模式,固件同时保留 UART 路径 |

---

## 附录 A:本仓库涉及文件

| 文件 | 角色 | 改动 |
|---|---|---|
| `next_stage/voice_sleep_v5_5090/voice_daemon.py` | 主流程 | 传输层替换为 Transport 接口 |
| `next_stage/voice_sleep_v5_5090/config.json` | 配置 | 新增 `network` 段 |
| `board_serial_asr_test.py` | 帧协议/测试 | `read_frame` 等重构为通用字节流 |
| `next_stage/full_pipeline_auto_5090/realtime_pipeline.py` | 下行命令 | `SPKS/SPKV/SPKD/SPKE` 不动,导出函数可复用 |
| `next_stage/voice_sleep_v5_5090/net_server.py` | **新增** | TLS 服务器 + 会话/帧封装 |
| 外部 `espidf-mic-pcm-stream` | 固件 | WiFi/TLS/会话 + 音频路径改造 |

## 附录 B:决策清单(开工前需拍板)

1. 公网出口:有公网 IP?还是走 Tailscale/云中转?
2. 证书:Let's Encrypt(DNS-01)还是阿里云免费证书?域名谁管?
3. mTLS:是(推荐,需固件烧证书)否(板 ID+密钥 HMAC 简化版)?
4. 多板卡:初期单板(推荐)还是将来多板?(影响会话 ID 设计)
5. 4G 卡流量:板端默认语音全在上传,按 30 min/天约 700 MB/月,是否已有流量方案?
