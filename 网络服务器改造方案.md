# 网络服务器改造方案 v2(多传输兼容 · 无固定 IP · 本地先行)

> **需求基线(最新)**:
> 1. **MQTT + HTTPS + WSS 三种传输都兼容**——固件侧已有成熟通信协议,待固件团队统计后回填本文档;
> 2. **不能依赖固定网络 IP**——板卡/服务器后续会更换网络环境;
> 3. **证书等通用件提前准备**——已完成:`certs/` 证书工具包(CA/服务器/板卡证书),见《certs/README_证书说明.txt》。
>
> 路线:**阶段一本地联调(无公网、无域名)→ 阶段二验收(客户服务器/平台,域名+证书)**。推理层(VAD/ASR/Qwen3/CosyVoice2)不动。

---

## 0. 结论摘要

1. **核心架构**:**统一会话中心(接入 → 读 IP 定位 → 分发)+ 流水线网关 + 三个传输适配器(MQTT / HTTPS / WSS)**。任何板卡连接先进会话中心登记:读对端 IP 记为"当前地址",确认身份后建立会话,再**分发到各适配器与推理网关**——三个通道共享同一会话,板卡从哪进回复从哪出,且可跨通道(网页 WSS 看同一会话、HTTP 下发命令)。
2. **无固定 IP 的解法**(关键):板卡与服务器**都主动出站**连接**稳定域名端点**(固件协议所在的平台端点,如 MQTT broker 域名)。双方 IP 随便换,只要域名不换就行;**身份用 board_id(证书/HMAC),绝不用 IP 白名单**;断线重连时**每次重新解析 DNS**,服务器侧证书 SAN 用域名而非 IP。
3. **证书已就绪**:`certs/` 已生成 CA(10 年)、服务器证书(2 年,SAN=localhost/本机 IP,可加域名)、板卡客户端证书 board-01(3 年);验收时换成客户域名证书即可。
4. 音频带宽:上行 32 KB/s、下行 48 KB/s(24 kHz TTS);MQTT/JSON 若走 base64 增加约 33% 流量,仍在任何 4G/WiFi 预算内。
5. 内部保留一个**统一帧格式 NET1**(二进制)作为网关内部交换格式与调试通道,与"固件侧协议"解耦:固件协议由适配器翻译,推理层永远只见 NET1。

---

## 1. 现状梳理(基准,不变)

- 板卡(ESP32-S3)→ USB 串口 921600 baud → `PCM1` 帧(16 kHz i16,20 ms,魔数+seq+len+dbfs×100+checksum8)+ `MICS/MICW` 命令;
- PC(`voice_daemon.py` V5 两段式唤醒)驱动全套:校准 → MICS 硬件休眠 → 唤醒词 → MICW 对话 → VAD → FunASR → Qwen3 流式 → CosyVoice2 → 下行 `SPKS/SPKV/SPKD/SPKE` → 板卡播放;
- 现状问题:串口物理链路 = 无会话、无鉴权、无重连,且板卡被 USB 线束缚。

---

## 2. 目标架构(多传输 · 无固定 IP)

```
            板卡(网络可换:WiFi/4G/…)
   ┌──────────────────┬──────────────────┬───────────────┐
   │ MQTT 客户端(平台) │ WSS 客户端        │ HTTPS 客户端   │
   └──────┬───────────┴────────┬─────────┴───────┬───────┘
          │  平台端点 = 稳定域名(现成协议栈,不换 IP 无影响)
   ┌──────▼────────────────────▼─────────▼───────┐
   │   ① 接入登记:读连接 IP → 记为"当前地址"       │
   └──────────────────────┬──────────────────────┘
   ┌──────────────────────▼──────────────────────┐
   │   ② 统一会话中心(Session Hub)                 │
   │      键 = board_id(ip_relax 时 = IP)          │
   │      分发:HTTPS │ WSS │ MQTT 适配器(只做翻译) │
   │      适配器注册表 ③ 会话事件 → 推理网关          │
   └──────────────────────┬──────────────────────┘
                          │ 归一化内部事件(音频帧/命令/状态)
   ┌──────────────────────▼──────────────────────┐
   │     流水线网关(会话状态机,板卡分离)             │
   │   会话=board_id:唤醒/对话/休眠/校准/断线恢复    │
   └──────────────────────┬──────────────────────┘
   ┌──────────────────────▼──────────────────────┐
   │   现有推理流水线(完全不动)                     │
   │  VAD → FunASR → Qwen3 → CosyVoice2 → TTS 块  │
   └─────────────────────────────────────────────┘
```

### 2.1 为什么这样能"无固定 IP"

| 传统做法(不可行) | 本方案 |
|---|---|
| 板卡直连 5090 的公网 IP(会换、无公网、被 NAT) | 板卡连**平台域名**,平台是稳定锚点 |
| 服务器 IP 白名单 | **board_id + 证书/HMAC** 鉴权,IP 不作身份 |
| 板卡缓存目标 IP | 每次连接**重新 DNS 解析**(域名不变) |
| 证书 SAN 写 IP(换 IP 就失效) | 证书 SAN 写**域名**;本地开发证书另含本机 IP 便于调试 |
| 服务器主动连板卡(找不到人) | 服务器**主动出站连平台**(订阅/推送),板卡同样出站 |

**结论:只要域名(平台端点/客户服务器域名)不换,板卡和服务器搬到哪里都一样。** 这正是"固件已有成熟协议"路线的最大红利——平台端点一般由平台方保证稳定。

### 2.2 两阶段

- **阶段一(本地联调)**:网关 + 三适配器 + 板卡模拟器都在本机跑通;板卡(若固件就绪)连局域网内的模拟/自建端点(IP 或临时主机名均可,只是测试,不构成依赖);不涉及公网/域名。
- **阶段二(验收)**:网关部署到客户服务器;客户提供平台/服务器域名与证书(或按 certs/ 工具重新签发);板卡固件只需改"连接地址 + 板卡证书",其余不变。

---

### 2.3 身份识别策略:能否按"连接 IP"判断?

> 结论:**IP 只作辅助,不作主身份**;开发阶段可开"IP 宽松直配"模式。

- **WSS/HTTPS 直连**:服务器能看到 TCP 对端(板卡)IP,可用于:
  - 会话定位/日志排查(哪台板卡哪次调用了);
  - 限速与防滥用(同一 IP 握手失败 N 次拉黑 X 分钟);
  - 局域网单板阶段快速直配(`lan_relax:true` 时,一条连接来了即认定为本机板卡,不验 board_id/证书)。
- **MQTT 通道**:服务器是 broker 的客户端,**看不到板卡 IP**(板卡 IP 在 broker 侧),IP 判断对该通道不适用 —— 这也是 MQTT 必须用 board_id 的原因。
- **换网络后**:"按 IP 判断"必然失效(WiFi↔4G、换路由器);NAT 后多板同 IP 也无法区分。**IP 是地址,不是身份;身份必须跨网络不变。**
- **推荐双因子**:
  - 主身份:`board_id`(HMAC 或证书 CN),跨网络稳定;
  - 辅助身份:当前连接 IP → 日志、限速、异常检测(同一 board_id 短时间从不同 IP 接入 → 告警);
  - 配置项:`config.json → network.identity: { mode: "board_id" | "ip_relax", ... }`;开发阶段 `ip_relax`(仅局域网+单板),验收阶段 `board_id`(顺带开启证书/HMAC),**切换只改配置,代码不动**。

---

## 3. 统一会话中心与传输适配层设计

> 三个适配器只做协议翻译;会话状态、身份、路由统一归**会话中心**管。音频一律透传字节(16 kHz i16 mono 板卡采集;24 kHz i16 mono 下行 TTS),不转码。

### 3.1 统一会话中心(Session Hub):接入登记 · 定位 · 分发

- **接入登记**(任何板卡连接建立后,第一步):
  1. **读对端 IP** → 记为会话元数据 `{peer_ip, transport, board_id, connected_at, last_seen}` —— 这就是"先读 IP,确认为当前地址":IP 只作**运行时定位**(谁在哪、日志排查、同 IP 限速),不作身份;
  2. **确认身份**:优先板卡自报 `board_id`;`ip_relax` 模式(局域网+单板)直接按 IP 认定,免烧板 ID/证书;
  3. **注册会话**:键 = `board_id`(或 ip_relax 时的 IP);同一板卡换通道/重连 = 同一会话(按 board_id 合并,LLM 历史按配置保留)。
- **分发**:适配器注册表 `{mqtt, wss, https}`;会话收到任一通道事件(音频帧/命令/状态)后转发给:
  - **回程通道**(默认:板卡从哪进,回复从哪出);
  - **推理网关**(唯一一份:音频进 VAD/ASR,回复 TTS 块经网关出);
  - **观察者通道**(可选跨通道互操作:浏览器经 WSS 订阅该会话做实时看板;HTTP 下发命令/查状态)。
- **MQTT 例外**:经 broker 时服务器看不到板卡 IP(peer_ip 记为 broker 地址),身份只能靠板卡自报 `board_id`;会话中心两种来源都收,优先级:**自报 id > IP 推测**。
- **生命周期**:注册 → 活动 → 断链/超时 → 注销(断链后保留 N 分钟"墓碑"供重连恢复,LLM history 按 board_id 保留,时长可配置)。

### 3.2 MQTT 适配器(推荐主通道,低延迟且双向流)

- 角色:MQTT **客户端**,连平台 broker(域名:端口,TLS 按平台要求),**注册到会话中心**,只做协议翻译与收发,不做会话状态;
- 主题命名建议(最终以固件为准,待统计回填):`board/{board_id}/audio`(上行)、`board/{board_id}/tts`(下行)、`board/{board_id}/cmd`(两者)、`board/{board_id}/status`;
- 音频载荷:二进制 PDU 优先;若平台消息体仅支持文本,则 **base64 + 小 JSON 头**(`{"seq":..,"dbfs":..,"b64":..}`),每块 20 ms(≈854 B base64);
- QoS:音频 QoS 0~1 均可(丢帧由网关容忍);命令/握手 QoS 1 + 保留消息(retain)做在线状态;
- 断线:自动重连(退避 1s→30s),重连后重新订阅与恢复会话状态(网关按 board_id 重建);
- 典型延迟:RTT 10–50 ms(看平台),抖动由网关缓冲吸收。

### 3.3 HTTPS 适配器(次要通道:文件级/低频率操作)

- 形态:REST API(基于 FastAPI/uvicorn,项目已有 CosyVoice 侧经验):
  - `POST /v1/audio/upload`(单次音频,base64 或 multipart)→ 返回识别+回答文本/音频文件;
  - `GET /v1/board/{id}/status`(在线/会话状态);
  - `POST /v1/board/{id}/command`(下发 MICS/MICW 等效命令);
  - 是否需要异步任务模式(`POST → task_id → GET result`)**取决于固件侧 HTTP 用法**,待统计;
- 鉴权:平台 token 或 mTLS(证书已备);不依赖来源 IP;
- 适合:非实时场景(网页调试、点评、慢速问答),实时对话不走这条路。

### 3.4 WSS 适配器(第三通道:浏览器/调试/双向流)

- 形态:WebSocket over TLS;`/ws/v1/board/{board_id}`;
- 上行:二进制帧直接放 PCM1 原帧(或 JSON+base64,视固件);下行:TTS 二进制块;
- 心跳:WS ping/pong + 应用层 5 s;
- 优势:浏览器可以直接测(前端演示、远程看板都用它),也是局域网联调的便捷通道。

### 3.5 内部统一格式 NET1(会话中心内部 + 调试 + 模拟器)

```
NET1 = "NET1"(4B) + payload_len(u32 LE) + type(1B) + payload
type: 0x01 音频帧(payload=原始 PCM1 帧字节流,含 magic/seq/dbfs/checksum)
      0x02 命令/数据(SPKS/SPKV/SPKD/SPKE/MICS/MICW 原字节)
      0x03 会话控制(握手/复位/错误)
      0x04/0x05 心跳 ping/pong
```
- **仅存在于会话中心内部与测试工具链**(板卡模拟器 ↔ 会话中心),对外由适配器翻译成固件协议;
- 若固件协议恰好也是二进制帧,适配器可退化为"透传映射",这是最省事情况(待统计确认)。

---

## 4. 证书与安全(已准备)

| 用途 | 文件 | 状态 |
|---|---|---|
| 信任锚 | `certs/ca/ca.crt`(F 10 年) | ✅ 已生成 |
| HTTPS/WSS/MQTT-TLS 服务端 | `certs/server/server.{crt,key}`(2 年,ECDSA) | ✅ 已生成(含本机 IP SAN) |
| 板卡 mTLS | `certs/clients/board-01.{crt,key}`(3 年) | ✅ 已生成 |
| 客户域名证书 | 验收时用客户证书替换 server 两张或按域名重签 | 待客户域名 |

安全要点:
- **身份 = board_id(证书 CN 或 HMAC),与 IP 无关**;
- 证书续期:CA 10 年/服务器 2 年/板卡 3 年,到期前 1 个月重签(certs/ 工具 + `verify`);
- 私钥(所有 *.key)已纳入 .gitignore,严禁进传输包;
- 本地阶段可明文(平台若有 TLS 则直接跟平台走),阶段二全程 TLS。

---

## 5. 服务器端实施清单(阶段一可完成部分)

1. **`next_stage/voice_sleep_v5_5090/session_hub.py`(新)**:统一会话中心——接入登记(读 IP)/身份确认/会话注册/分发/生命周期,适配器注册表;
2. **`next_stage/voice_sleep_v5_5090/gateway.py`(新)**:流水线网关——语音会话状态机(handshake/校准/唤醒/对话/休眠),板卡按 board_id 隔离,输入=会话中心事件,输出=TTS 块/命令;
3. **`adapters/`(新)**:`mqtt_adapter.py`(paho-mqtt 或平台 SDK)、`wss_adapter.py`(FastAPI/uvicorn + websockets)、`https_adapter.py`(FastAPI);三者共用同一事件接口,注册到会话中心;
4. **`voice_daemon.py` 微改**:模型加载、VAD/ASR/Qwen/TTS 抽取为可被网关调用的服务(现有逻辑保留,只是调度主体换成网关);
5. **`board_simulator.py`(新)**:模拟板卡(MQTT/WSS/HTTPS 三模式可选),注入抖动/丢帧/断线,自动化验收;
6. **`config.json` 扩展**:`network.identity`(board_id/ip_relax)+ `transport: {mqtt:{...}, wss:{...}, https:{...}}` 每项含 enabled/host/port/tls/证书/板卡列表;
7. 复用全部现有测试脚本与 CSV 指标(网络版只需换输入源)。

> 说明:适配器与网关先把**接口定死再实现**:事件 = `AudioFrame(seq, dbfs, samples)` / `Command(bytes)` / `SessionEvent(board_id, kind)`,这样固件协议统计回来只改适配器内部,网关与推理层零改动。

---

## 6. 延迟/带宽核算

| 项 | 数值 | 备注 |
|---|---|---|
| 上行音频 | 32 KB/s(16k×16 bit) | MQTT/JSON base64 后 ≈43 KB/s |
| 下行 TTS | 48 KB/s(24k×16 bit) | base64 后 ≈64 KB/s |
| 平台 RTT | 10–50 ms(常态) | 实测为准 |
| 抖动缓冲 | 上/下行 50–120 ms | 网关内统一,可调 |
| Qwen 首字/首段 | 与串口版本一致 | 推理侧不变,网络侧多 ~1 RTT |

---

## 7. 测试与验收

- **阶段一(现在可做)**:网关 + 模拟器三通道自测(握手/心跳/断线重连/丢帧/乱序);与串口基线比 8 句 CER/延迟;
- **阶段二(验收)**:客户环境部署;域名+证书;准入测试(无证书拒连、错误 board_id 拒连、断线恢复);1 小时长稳;
- **红线**:CER 较串口基线差 ≤0.02;无 TTS 爆音/卡顿;30 分钟内断线恢复 ≥1 次。

---

## 8. 里程碑(更新)

| # | 阶段 | 内容 | 依赖 | 工作量 |
|---|---|---|---|---|
| M0 | 资料 | 固件协议统计回填(见附录 B) | 固件团队 | — |
| M1 | 本地 | 网关 + 三适配器框架(接口定死)+ config | M0 后回填细节 | 3–4 |
| M2 | 本地 | 板卡模拟器(三模式)+ 自动化自测 | M1 | 1–2 |
| M3 | 本地 | 真实板卡联调(固件就绪后) | 固件 | 1–2 |
| M4 | 验收 | 客户部署/证书/准入/长稳 | M0–M3 | 1–2 |

---

## 9. 风险与回退

| 风险 | 缓解 |
|---|---|
| 固件协议资料未到 | M1 先把网关与适配器接口定死;到料后只填适配器内部 |
| 平台 MQTT 消息体限制(纯文本) | 主推 WSS/二进制;或 base64+分块 |
| 板卡 IP 频繁变化 | 域名寻址 + board_id 鉴权 + 重连重解析,已内建 |
| 阶段二证书/域名缺口 | 与客户提前确认;certs/ 工具可临时自签过渡 |
| 多板卡 | 网关按 board_id 会话隔离,天然支持 |
| 任何故障 | 保留串口模式兜底(现有 V5 不变) |

---

## 附录 A:文件清单(变更)

| 文件 | 角色 |
|---|---|
| `网络服务器改造方案.md` | 本文档 |
| `certs/generate_certs.py` + `certs/README_证书说明.txt` | 证书工具包(✅ 已生成一套) |
| `next_stage/voice_sleep_v5_5090/session_hub.py`(待建) | 统一会话中心(登记/定位/分发) |
| `next_stage/voice_sleep_v5_5090/gateway.py`(待建) | 流水线网关 |
| `next_stage/voice_sleep_v5_5090/adapters/*`(待建) | MQTT/WSS/HTTPS 适配器 |
| `next_stage/voice_sleep_v5_5090/board_simulator.py`(待建) | 板卡模拟器 |
| `next_stage/voice_sleep_v5_5090/voice_daemon.py`(微改) | 推理服务宿主 |
| `next_stage/voice_sleep_v5_5090/config.json`(扩展) | 传输/证书/板卡配置 |

## 附录 B:待固件侧统计/确认清单(等你们资料)

**A. MQTT**(若有):
1. broker 域名:端口、TLS 类型(证书/双向);
2. 认证方式(client_id? username/password? 证书?)、是否需要注册流程;
3. 主题命名与各主题的消息格式(二进制?JSON+base64?)、分块大小、QoS;
4. 设备影子/属性上报是否必须(若平台架构强制);
5. 会话保持(clean_session=false?)与离线消息要求。

**B. HTTPS**(若有):
6. 接口清单与路径(上传/下发/查询)、鉴权方式(token/cookie/证书);
7. 请求/响应体格式(音频编码、是否轮询任务);
8. 是否允许"服务器主动下发"还是只能板卡轮询(影响对话实时性 → 若只能轮询,对话延迟/体验要重新评估)。

**C. WSS**(若有):
9. 端点地址、握手首帧格式、二进制 or 文本帧、心跳方式;
10. 双向流是否按"请求-应答"还是持续流(决定网关缓冲策略)。

**D. 通用**:
11. 音频参数:采样率/位深/声道(预计 16k/16bit/mono,请确认)与 TTS 播放端(板载 I2S?服务器端播放?);
12. 唤醒/休眠状态机:是否仍在板端本地?`MICS/MICW` 等效命令如何映射;
13. 板卡时钟/NTP、时间戳字段是否需要;
14. 切换网络(4G↔WiFi)时板卡行为:自动重连?IP 变更通知?

## 附录 C:开工决策(阶段一)

1. 网关/推理服务跑在 5090(推荐,模型已就位);
2. 阶段一模拟器优先打通 **MQTT(或固件主通道)**,其余两通道按 M0 资料决定优先级;
3. 域名:阶段二由客户提供(尽量提前要,证书签发有 2 年有效期,不影响本地开工)。
